export interface TimelineEvent {
  caption: string;
  date: Date;
  title: string;
  selected?: boolean;
  content: string;
}
export interface TimelineElement {
  caption: string;
  date: Date;
  title: string;
  selected?: boolean;
  content: string;
}
